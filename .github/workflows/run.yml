name: fetch-data
on:
  schedule:
    - cron: "0 0 */2 * *" # Schedule to run every 2 days at midnight (UTC)
jobs:
  my-job:
    name: my-job
    runs-on: ubuntu-latest
    steps:
      # ...
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v1

      # run any `bun` or `bunx` command
      - run: bun install
      - run: bun index.ts
